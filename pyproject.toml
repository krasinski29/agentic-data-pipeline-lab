[project]
name = "pipeline-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "krasinski29", email = "49124459+krasinski29@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
pipeline-lab = "pipeline_lab:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
    "pytest>=9.1.1",
    "ruff>=0.16.7",
]
